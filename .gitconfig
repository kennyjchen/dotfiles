[user]
	name = Kenny Chen
	email = kchen@exyntechnologies.com
	username = kchen
[core]
	editor = nvim
	excludesFile = ~/.gitignore_global
[color]
	ui = auto
[alias]
	# Shortening aliases
  aa = add --all
  ap = add --patch
	cp = "!f() { git commit -m \"$1\" && git pull --rebase && git push; }; f"
	d = diff
  df = diff --cached
	st = status -sb
	rv = remote -v
  rh = reset --hard

	# Feature improving aliases
	rank = shortlog -sn --no-merges
	untracked = status --short --untracked-files
	aliases = !git config --get-regexp ^alias\\. | sed -e s/^alias\\.// -e s/\\ /\\ =\\ /

	# Log
	lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
	lga = log --all --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit

