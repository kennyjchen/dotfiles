[user]
	name = Kenny Chen
	email = kennyjchen@ucla.edu
	username = kennyjchen
	signingKey = 89D26BECC9B44BEE
[core]
	editor = vim
	excludesFile = ~/.gitignore_global
[commit]
	gpgSign = true
[color]
	ui = auto
[alias]
	# Shortening aliases
	co = checkout
	cob = checkout -b
	a = add
	f = fetch
	fp = fetch -p
	pl = pull
	c = commit
	cm = commit -m
	ps = push
	ba = branch -a
	bd = branch -d
	d = diff
	dc = diff --cached

	# Feature improving aliases
	st = status -sb
	ap = add -p
	aa = add --all
	acp = !git add --all && git commit -m "$1" && git push && :
	rank = shortlog -sn --no-merges
	alias = !git config --get-regexp ^alias\\. | sed -e s/^alias\\.// -e s/\\ /\\ =\\ /

	# Log
	lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit

	# Cleanup aliases
	archivebranch = !git checkout $1 && git switch develop && git tag archive/$1 $1 && git branch -D $1 && git push -d origin $1 && git push --tags && :
	restorebranch = !git checkout -b $1 archive/$1 && :
